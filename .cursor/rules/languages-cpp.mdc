---
description: 
globs: **/*.c,**/*.cpp,**/*.h,**/*.hpp,**/*.cxx,CMakeLists.txt,*.cmake,conanfile.txt,Makefil,**/*.cc
alwaysApply: false
---

# C++ 编程指南

## 基本原则
- 所有代码和文档均使用英语。
- 始终声明每个变量和函数（参数和返回值）的类型。
- 创建必要的类型和类。
- 使用 Doxygen 风格的注释来记录公共类和方法。
- 函数内部不要留空行。
- 遵循单一定义规则（ODR）。

## 命名规范
- 类和结构体使用帕斯卡命名法（PascalCase）。
- 变量、函数和方法使用驼峰命名法（camelCase）。
- 常量和宏使用全大写命名法（ALL_CAPS）。
- 文件和目录名使用蛇形命名法（snake_case）。
- 环境变量使用大写字母命名。
- 避免使用魔法数字，要定义常量。
- 每个函数名以动词开头。
- 布尔型变量使用动词命名。例如：isLoading、hasError、canDelete 等。
- 使用完整的单词，而非缩写，并确保拼写正确。
  - 标准缩写（如 API、URL 等）除外。
  - 常用缩写除外：
    - 循环使用 i、j、k。
    - 错误使用 err。
    - 上下文使用 ctx。
    - 请求/响应参数使用 req、res。

## 函数
- 编写具有单一用途的简短函数。指令数少于 20 条。
- 函数名由动词和其他内容组成。
- 如果函数返回布尔值，使用 isX、hasX、canX 等命名。
- 如果函数无返回值（void），使用 executeX、saveX 等命名。
- 通过以下方式避免代码块嵌套：
  - 提前检查并返回。
  - 提取为工具函数。
- 使用标准库算法（如 std::for_each、std::transform、std::find 等）避免函数嵌套。
- 简单操作使用 lambda 函数。
- 非简单操作使用具名函数。
- 使用默认参数值，而不是检查空指针或 nullptr。
- 使用结构体或类减少函数参数：
  - 使用对象传递多个参数。
  - 使用对象返回多个结果。
  - 为输入参数和输出声明必要的类型。
- 保持抽象层次单一。

## 数据
- 不要滥用基本数据类型，将数据封装在复合类型中。
- 避免在函数中进行数据验证，使用具有内部验证机制的类。
- 数据优先采用不可变性。
- 对于不变的数据使用 const。
- 编译时常量使用 constexpr。
- 可能为空的值使用 std::optional。

## 类
- 遵循 SOLID 原则。
- 优先使用组合而非继承。
- 将接口声明为抽象类或概念。
- 编写具有单一用途的小类：
  - 指令数少于 200 条。
  - 公共方法少于 10 个。
  - 属性少于 10 个。
- 使用五法则（或零法则）进行资源管理。
- 将成员变量设为私有，必要时提供 getter/setter 方法。
- 成员函数使用 const 正确性。

## 异常
- 使用异常处理意外错误。
- 捕获异常时，目的应为：
  - 解决预期问题。
  - 补充上下文信息。
  - 否则，使用全局处理器。
- 对于预期的失败，使用 std::optional、std::expected 或错误码。

## 内存管理
- 优先使用智能指针（std::unique_ptr、std::shared_ptr）而非原始指针。
- 使用 RAII（资源获取即初始化）原则。
- 通过合理的资源管理避免内存泄漏。
- 使用 std::vector 和其他标准容器，而非 C 风格数组。

## 测试
- 测试遵循 “排列 - 行动 - 断言”（Arrange - Act - Assert）规范。
- 清晰命名测试变量。
- 遵循如下规范：inputX、mockX、actualX、expectedX 等。
- 为每个公共函数编写单元测试。
- 使用测试替身模拟依赖项。
  - 执行成本不高的第三方依赖项除外。
- 为每个模块编写集成测试。
- 遵循 “给定 - 当 - 那么”（Given - When - Then）规范。

## 项目结构
- 使用模块化架构。
- 将代码组织到逻辑目录中：
  - include/ 用于存放头文件。
  - src/ 用于存放源文件。
  - test/ 用于存放测试文件。
  - lib/ 用于存放库文件。
  - doc/ 用于存放文档。
- 使用 CMake 或类似的构建系统。
- 将接口（.h）与实现（.cpp）分离。
- 使用命名空间对代码进行逻辑组织。
- 为基础组件创建一个核心命名空间。
- 为工具函数创建一个工具命名空间。

## 标准库
- 尽可能使用 C++ 标准库。
- 优先使用 std::string 而非 C 风格字符串。
- 使用 std::vector、std::map、std::unordered_map 等进行集合操作。
- 使用 std::optional、std::variant、std::any 实现现代类型安全。
- 使用 std::filesystem 进行文件操作。
- 使用 std::chrono 进行时间相关操作。

## 并发
- 使用 std::thread、std::mutex、std::lock_guard 确保线程安全。
- 优先使用基于任务的并行，而非基于线程的并行。
- 使用 std::atomic 进行原子操作。
- 通过合理的同步避免数据竞争。
- 必要时使用线程安全的数据结构。 